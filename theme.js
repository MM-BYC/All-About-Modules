/* Color scheme - sets the color of the argument element.
    Usage: colorScheme(<arg>) 
        Define import: import { colorScheme } from './theme.js';
        Call function: 
        const myElement = document.getElementById('my-Element');
        colorScheme(my-Element);
*/

export const colorScheme = {
  mainBg: "#4a4e4d",
  topMenuBg: "#0e9aa7",
  subMenuBg: "#3da4ab",
};
